// @ts-check
import { defineConfig } from 'astro/config';

// https://astro.build/config
export default defineConfig({
  site: 'https://chadjarreau.github.io',
  base: '/signals-from-tau-ceti/',
  output: 'static',
});
